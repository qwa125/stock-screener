import { OnApplicationBootstrap } from '@nestjs/common';
import { DataFetcherService } from '../stock/data-fetcher.service';
import { SectorHotResponse } from './sector.types';
import { GemScreenerService } from '../gem-screener/gem-screener.service';
export declare class SectorService implements OnApplicationBootstrap {
    private readonly dataFetcher;
    private readonly gemScreener;
    private readonly logger;
    private cache;
    private loadingPromise;
    private readonly CACHE_TTL;
    private readonly FROZEN_TTL;
    private readonly CACHE_FILE;
    private refreshing;
    private consCache;
    private readonly CONS_CACHE_TTL;
    private refreshTimer;
    constructor(dataFetcher: DataFetcherService, gemScreener: GemScreenerService);
    private getEffectiveTTL;
    private saveCacheToDisk;
    private createEmptyResponse;
    private loadCacheFromDisk;
    onApplicationBootstrap(): Promise<void>;
    private startAutoRefresh;
    private stopAutoRefresh;
    private mergeSectorData;
    getHotSectors(): Promise<SectorHotResponse>;
    private refreshCacheInBackground;
    private doRefresh;
    private fillLeadingStocks;
    private getLeadingStocks;
    private checkBaiXiaoBatch;
    private checkSingleBaiXiao;
    private getSectorConstituents;
    private fetchStockQuotes;
    private formatDateTime;
    private fetchAllSectorKLines;
    private fetchSectorKLine;
    private fetchRealtimePrices;
    private checkMoneyFlowBatch;
    private getSingleMoneyFlow;
}
